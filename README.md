# react-list-ui

Ready-to-run React + Tailwind project (UI with hover, shadow, nice cards).
Uses public API: https://jsonplaceholder.typicode.com/users

## Quick start

1. Install dependencies:
   ```bash
   npm install
   ```

2. Start dev server:
   ```bash
   npm start
   ```

3. Open http://localhost:3000

